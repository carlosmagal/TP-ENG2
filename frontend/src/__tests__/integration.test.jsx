import React from "react";
// import { render, screen } from "@testing-library/react";

describe("first", () => {
  it("should render the component im testing", () => {
    // render(<div />);
    expect(1).toBe(1);
  });
});
